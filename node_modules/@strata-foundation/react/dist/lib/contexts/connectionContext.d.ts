import React, { FC, ReactNode } from "react";
import { Connection } from "@solana/web3.js";
export interface IConnectionProviderProps {
    children: ReactNode;
}
export interface IConnectionContextState {
    endpoint: string;
    setEndpoint: (val: string) => void;
    connection: Connection;
}
export declare const ConnectionContext: React.Context<IConnectionContextState>;
export declare const ConnectionProvider: FC<IConnectionProviderProps>;
//# sourceMappingURL=connectionContext.d.ts.map